name: Main

on:
 workflow_dispatch 
 
jobs:

 CheckoutRepo:
 
  runs-on: [self-hosted, windows, x64]
  steps:
      
    - uses: actions/checkout@v3.3.0
    
    #- uses: ./.github/workflows/ReplaceTokens.yml
    
 
 ReplaceTokens:
 
  needs: CheckoutRepo
  
  uses: ./.github/workflows/ReplaceTokens.yml
  with:
   target_environment: "Dev"
   
    
  
  
  #- name: Replace tokens
   # env: 
    # Name: ${{vars.NAME}}
     #Age: ${{vars.AGE}}
    # Profession: ${{vars.PROFESSION}}
     
     #functionNumber1 same as 3
    #uses: cschleiden/replace-tokens@v1.2
    #with: 
     #tokenPrefix: '{{'
     #tokenSuffix: '}}'
     #files: 'MyArtifacts/Artifactone'
     
    #functionNumber2 on linux
    #uses: falnyr/replace-env-vars-action@v1.3.2
    #with:
     #filename: MyArtifacts/Artifactone
     
  #functionNumber3
    #uses: 4lch4/replace-tokens@v1.1.0
    #with:
    
     #tokenPrefix: '{{'
     #tokenSuffix: '}}'
     #files: MyArtifacts/Artifactone
     
   #functionNumber4(Linux)
    #uses: sandersaares-actions/expand-tokens@2020-01-28
    #with:
    # Path to the file or directory to process.
    # path: MyArtifacts/Artifactone
     
     
   #functionNumber5
    #uses: nightstory/replace-env@v1
    #with:
    # Input file path
     #input_file: MyArtifacts/Artifactone
     
    
    
